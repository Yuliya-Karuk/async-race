export enum RaceViews {
  garage = 'garage',
  winners = 'winners',
}

export enum SortBy {
  'id' = 'id',
  'wins' = 'wins',
  'time' = 'time',
}

export enum Order {
  'ASC' = 'ASC',
  'DESC' = 'DESC',
}
